apiVersion: v1
kind: Service
metadata:
  name: team5-backend
  namespace: skala3-finalproj-class2-team5
  labels:
    app: team5-backend
spec:
  type: ClusterIP
  selector:
    app: team5-backend
  ports:
    - name: http
      port: 8000
      targetPort: http
