apiVersion: v1
kind: Service
metadata:
  name: team5-vision
  namespace: skala3-finalproj-class2-team5
  labels:
    app: team5-vision
spec:
  type: ClusterIP
  selector:
    app: team5-vision
  ports:
    - name: http
      port: 8002
      targetPort: http
