apiVersion: v1
kind: Service
metadata:
  name: team5-postgres
  namespace: skala3-finalproj-class2-team5
  labels:
    app: team5-postgres
spec:
  type: ClusterIP
  selector:
    app: team5-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
