apiVersion: v1
kind: Service
metadata:
  name: team5-redis
  namespace: skala3-finalproj-class2-team5
  labels:
    app: team5-redis
spec:
  type: ClusterIP
  selector:
    app: team5-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
