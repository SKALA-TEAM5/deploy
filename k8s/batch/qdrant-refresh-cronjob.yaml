apiVersion: batch/v1
kind: CronJob
metadata:
  name: team5-qdrant-refresh-cronjob
  namespace: skala3-finalproj-class2-team5
  labels:
    app: team5-batch
    job: qdrant-refresh-cronjob
spec:
  schedule: "30 22 * * *"  # 07:30 KST (UTC+9)
  timeZone: "Asia/Seoul"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        metadata:
          labels:
            app: team5-batch
            job: qdrant-refresh-cronjob
        spec:
          restartPolicy: Never
          imagePullSecrets:
            - name: team5-harbor-secret
          containers:
            - name: batch
              image: amdp-registry.skala-ai.com/skala26a-ai2/team5-batch:latest
              imagePullPolicy: Always
              env:
                - name: QDRANT_URL
                  value: http://team5-qdrant:6333
                - name: POSTGRES_HOST
                  value: team5-postgres
                - name: POSTGRES_PORT
                  value: "5432"
                - name: POSTGRES_DB
                  value: safety
                - name: QDRANT_COLLECTION
                  value: legal_documents
                - name: LAW_APP_USER
                  valueFrom:
                    secretKeyRef:
                      name: team5-postgres-secret
                      key: LAW_APP_USER
                - name: LAW_APP_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: team5-postgres-secret
                      key: LAW_APP_PASSWORD
                - name: LAW_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: team5-batch-secret
                      key: LAW_API_KEY
              command:
                - sh
                - -c
                - |
                  export DATABASE_URL="postgresql://${LAW_APP_USER}:${LAW_APP_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}"
                  python -m src.refresh_service --collection "${QDRANT_COLLECTION}"
              resources:
                requests:
                  cpu: 500m
                  memory: 1Gi
                limits:
                  cpu: "1"
                  memory: 2Gi
