apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: team5-prometheus-data
  namespace: skala3-finalproj-class2-team5
  labels:
    app: team5-prometheus
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 5Gi
