apiVersion: v1
kind: Service
metadata:
  name: team5-prometheus
  namespace: skala3-finalproj-class2-team5
  labels:
    app: team5-prometheus
spec:
  type: ClusterIP
  selector:
    app: team5-prometheus
  ports:
    - name: http
      port: 9090
      targetPort: http
