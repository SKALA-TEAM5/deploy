apiVersion: v1
kind: Service
metadata:
  name: team5-grafana
  namespace: skala3-finalproj-class2-team5
  labels:
    app: team5-grafana
spec:
  type: ClusterIP
  selector:
    app: team5-grafana
  ports:
    - name: http
      port: 3000
      targetPort: http
